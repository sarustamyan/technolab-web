@model Technolab.OnlineLibrary.Web.ViewModels.PagerViewModel
@{
    var controller = ViewData["controller"];
    var action = ViewData["action"];
}
<div class="container">
   @if (Model.TotalPages > 0)
   {
     <ul class="pagination justify-content-end">
         @if (Model.CurrentPage > 1)
         {
             <li class="page-item">
                 <a class="page-link" asp-controller=@controller asp-action=@action asp-route-page="1">
                     First
                 </a>
             </li>
             <li class="page-item">
                 <a class="page-link" asp-controller=@controller asp-action=@action asp-route-page="@(Model.CurrentPage-1)">
                     Previous
                 </a>
             </li>
         }
         @for (var pageIndex = Model.StartPage; pageIndex <= Model.EndPage; pageIndex++)
         {
             <li class="page-item @(pageIndex == Model.CurrentPage ? "active" : "")">
                 <a class="page-link" asp-controller=@controller asp-action=@action asp-route-page="@pageIndex">
                     @pageIndex
                 </a>

             </li>
         }
         @if (Model.CurrentPage < Model.TotalPages)
         {
             <li class="page-item">
                 <a class="page-link" asp-controller=@controller asp-action=@action asp-route-page="@(Model.CurrentPage+1)">
                     Next
                 </a>
             </li>
             <li class="page-item">
                 <a class="page-link" asp-controller=@controller asp-action=@action asp-route-page="@Model.TotalPages">
                     Last
                 </a>
             </li>
         }
     </ul>
 }
</div>